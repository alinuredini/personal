<section class="hero">
  <div class="hero-inner">
    <h1 class="hero-name">
      <span class="name-line">Ali</span>
      <span class="name-line">Nuredini</span>
    </h1>
    <p class="hero-tagline">
      Germany based Designer with an infinite love for photography,
      specialised in <em>UX + UI</em> &amp; <em>DesignOps</em>.
    </p>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 6rem 2rem 4rem;
    position: relative;
  }

  .hero-inner {
    max-width: 1200px;
    width: 100%;
    text-align: center;
  }

  .hero-name {
    font-family: var(--font-display);
    font-weight: 700;
    line-height: 0.9;
    letter-spacing: -0.04em;
    margin-bottom: 2.5rem;
    overflow: hidden;
  }

  .name-line {
    display: block;
    font-size: clamp(4rem, 15vw, 14rem);
    animation: slideUp 1s var(--ease-out-expo) both;
  }

  .name-line:nth-child(2) {
    animation-delay: 0.15s;
  }

  .hero-tagline {
    font-family: var(--font-sans);
    font-size: clamp(1rem, 2vw, 1.35rem);
    font-weight: 300;
    color: var(--color-text-muted);
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.6;
    animation: fadeIn 1s ease 0.5s both;
  }

  .hero-tagline em {
    font-style: italic;
    color: var(--color-text);
  }

  @keyframes slideUp {
    from {
      transform: translateY(100%);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 5rem 1.5rem 3rem;
    }

    .hero-tagline {
      max-width: 90%;
    }
  }
</style>
