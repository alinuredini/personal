<header class="header">
  <span class="header-left">® 2026</span>
  <a href="#contact" class="header-right">CONTACT</a>
</header>

<style>
  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 24px 48px;
    position: relative;
    z-index: 10;
  }

  .header-left {
    font-family: var(--font-display);
    font-size: 12px;
    font-weight: 400;
    color: var(--color-white);
  }

  .header-right {
    font-family: var(--font-display);
    font-size: 12px;
    font-weight: 400;
    color: var(--color-white-70);
    letter-spacing: -0.12px;
    transition: color 0.3s ease;
  }

  .header-right:hover {
    color: var(--color-white);
  }

  @media (max-width: 768px) {
    .header {
      padding: 16px 24px;
    }
  }
</style>
