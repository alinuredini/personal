<section class="current-work">
  <div class="cw-left">
    <p class="cw-label">These days you can find me working as</p>
  </div>
  <div class="cw-right">
    <a href="https://8-reasons.com" target="_blank" rel="noopener noreferrer" class="cw-role">DesignOps Lead @ 8reasons</a>
    <a href="https://pistacio.app/" target="_blank" rel="noopener noreferrer" class="cw-role">Co-Founder @ Pistacio</a>
  </div>
</section>

<style>
  .current-work {
    width: 100%;
    max-width: 1100px;
    margin: 0 auto;
    background: var(--color-green);
    border-radius: 20px;
    padding: 64px;
    display: flex;
    flex-direction: row;
    align-items: flex-start;
    gap: 48px;
  }

  .cw-left {
    flex: 1;
  }

  .cw-label {
    font-family: var(--font-sans);
    font-size: 32px;
    font-weight: 400;
    color: var(--color-purple);
    line-height: 1.2;
  }

  .cw-right {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 8px;
    text-align: right;
  }

  .cw-role {
    font-family: var(--font-sans);
    font-size: 32px;
    font-weight: 400;
    color: var(--color-purple);
    line-height: 1.2;
    transition: opacity 0.3s ease;
  }

  .cw-role:hover {
    opacity: 0.6;
  }

  @media (max-width: 900px) {
    .current-work {
      flex-direction: column;
      padding: 40px;
      gap: 32px;
    }

    .cw-label,
    .cw-role {
      font-size: clamp(22px, 3.5vw, 32px);
    }

    .cw-right {
      text-align: left;
    }
  }
</style>
