<section class="current-work">
  <div class="current-work-inner">
    <p class="section-label">These days you can find me working as</p>
    <div class="roles">
      <a href="https://8-reasons.com" target="_blank" rel="noopener noreferrer" class="role-card role-card--purple">
        <div class="role-content">
          <span class="role-title">DesignOps Lead</span>
          <span class="role-at">@ 8reasons</span>
        </div>
        <svg class="role-arrow" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M7 17L17 7" />
          <path d="M7 7h10v10" />
        </svg>
      </a>
      <a href="https://pistacio.app/" target="_blank" rel="noopener noreferrer" class="role-card role-card--olive">
        <div class="role-content">
          <span class="role-title">Co-Founder</span>
          <span class="role-at">@ Pistacio</span>
        </div>
        <svg class="role-arrow" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M7 17L17 7" />
          <path d="M7 7h10v10" />
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .current-work {
    padding: 4rem 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .current-work-inner {
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    padding-top: 3rem;
  }

  .section-label {
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 400;
    color: var(--color-text-muted);
    margin-bottom: 1.5rem;
  }

  .roles {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }

  .role-card {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 2rem 2.5rem;
    border-radius: 16px;
    transition: all 0.4s var(--ease-out-expo);
    position: relative;
    overflow: hidden;
  }

  .role-card::after {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 80% 80%, rgba(255,255,255,0.06), transparent 60%);
    opacity: 0;
    transition: opacity 0.4s ease;
  }

  .role-card:hover::after {
    opacity: 1;
  }

  .role-card--purple {
    background: var(--color-purple);
  }

  .role-card--olive {
    background: var(--color-olive-alt);
  }

  .role-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
  }

  .role-content {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    position: relative;
    z-index: 1;
  }

  .role-title {
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  .role-at {
    font-family: var(--font-sans);
    font-size: 1rem;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.7);
  }

  .role-arrow {
    position: relative;
    z-index: 1;
    opacity: 0.5;
    transform: translate(-4px, 4px);
    transition: all 0.35s var(--ease-out-quart);
  }

  .role-card:hover .role-arrow {
    opacity: 1;
    transform: translate(0, 0);
  }

  @media (max-width: 768px) {
    .current-work {
      padding: 3rem 1.5rem;
    }

    .roles {
      grid-template-columns: 1fr;
    }

    .role-card {
      padding: 1.5rem 2rem;
    }
  }
</style>
